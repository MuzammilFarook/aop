spring.application.name=aop
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
#Currently, the retry configuration manually retrieves the Retry instance from RetryRegistry, which doesn't support dynamic updates when properties change. Instead, consider using @ConfigurationProperties to bind retry settings and RetryRegistryCustomizer to automatically refresh configurations on /actuator/refresh. This would eliminate the need for manual property fetching and ensure retry settings update dynamically without restarting the app. Also, you can attach event listeners dynamically when creating the retry instance to maintain logging behavior. Let me know if you need any guidance!
